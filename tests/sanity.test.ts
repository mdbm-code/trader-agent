/**
 * ────────────────────────────────────────────────────────────────────────────
 * ФАЙЛ:          tests/sanity.test.ts
 * СЛОЙ:          — (проверка инструмента, не кода)
 * РОЛЬ:          ВРЕМЕННЫЙ: доказывает, что vitest настроен и находит тесты.
 *                Удаляется в уроке 2, когда появится первый настоящий тест
 *                (app-error.test.ts).
 * ПАТТЕРН:       —
 * ИСПОЛЬЗУЕТСЯ:  package.json → npm run test
 * ТЕСТЫ:         это и есть тест
 * 1С:            —
 * ────────────────────────────────────────────────────────────────────────────
 */
import { expect, test } from 'vitest';

test('vitest настроен', () => {
  expect(1 + 1).toBe(2);
});
