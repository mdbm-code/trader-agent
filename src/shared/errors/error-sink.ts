/**
 * ────────────────────────────────────────────────────────────────────────────
 * ФАЙЛ:          src/shared/errors/error-sink.ts
 * СЛОЙ:          Общее ядро (shared) → ошибки, порт
 * РОЛЬ:          Контракт одного приёмника нормализованного события об ошибке:
 *                консоль, позже — Telegram, Max владельцу и т.п.
 * ПАТТЕРН:       Порт — второй в модуле. Первый, ErrorReporter (урок 2), смотрит
 *                наружу модуля; этот смотрит внутрь — между CompositeErrorReporter
 *                и конкретным каналом оповещения
 * ИСПОЛЬЗУЕТСЯ:  CompositeErrorReporter, ConsoleErrorSink (оба — урок 3)
 * ТЕСТЫ:         — (чистый интерфейс, нет поведения, которое можно проверить в рантайме)
 * 1С:            переопределяемый модуль БСП для конкретного канала оповещения
 * ────────────────────────────────────────────────────────────────────────────
 */
import type { Severity } from './app-error.ts';
import type { ErrorEvent } from './error-event.ts';

export interface ErrorSink {
  // Пускать ли это событие вообще — например, чтобы не будить Telegram на 'info'.
  // Единственная сегодняшняя реализация (ConsoleErrorSink) всегда отвечает true:
  // фильтровать пока не от чего, смысл появится со вторым приёмником.
  accepts(severity: Severity): boolean;
  send(event: ErrorEvent): Promise<void>;
}
