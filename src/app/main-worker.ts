/**
 * ────────────────────────────────────────────────────────────────────────────
 * ФАЙЛ:          src/app/main-worker.ts
 * СЛОЙ:          app (сборка и запуск)
 * РОЛЬ:          Точка входа торгового процесса.
 *                ЗАГОТОВКА: наполняется в уроке 13 (Composition Root) —
 *                пока только доказывает, что tsx watch поднимает TS-файл
 *                без ошибок компиляции.
 * ПАТТЕРН:       —
 * ИСПОЛЬЗУЕТСЯ:  package.json → npm run dev
 * ТЕСТЫ:         —
 * 1С:            пустой модуль регламентного задания — уже в расписании,
 *                но пока ничего не делает
 * ────────────────────────────────────────────────────────────────────────────
 */
console.log('trader-agent: dev-контур жив');
